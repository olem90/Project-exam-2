import styled from "styled-components";
import { Nav } from "../Nav/Nav";
import { HeaderStyles } from "./Header.styles";

export const Header = () => {
  return (
    <HeaderStyles>
      <Nav />
    </HeaderStyles>
  );
};
