import styled from "styled-components"

export const HeaderStyles = styled.header`
  min-width: 100%;
  position: absolute;
  top: 0; 
  
    img {
      max-width: 100%;
      min-width: 100%;
      max-height: 400px;
      object-fit: cover;
      filter: brightness(50%);  
    }
` 