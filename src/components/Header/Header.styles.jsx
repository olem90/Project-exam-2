import styled from "styled-components"

export const HeaderStyles = styled.header`
  border: 2px solid red;

    img {
      max-width: 100%;
      min-width: 100%;
      max-height: 400px;
      object-fit: cover;
    }
`